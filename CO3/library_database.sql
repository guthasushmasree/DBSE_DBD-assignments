CREATE DATABASE IF NOT EXISTS library_db;
USE library_db;

-- Spring Boot will create/update the tables automatically.
-- Run this script in MySQL Workbench before starting Spring Boot.

SELECT DATABASE();
